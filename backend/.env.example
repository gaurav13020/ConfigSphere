DJANGO_SETTINGS_MODULE=config_sphere.settings.local
DJANGO_SECRET_KEY=replace-me-with-a-real-secret-key

# PostgreSQL (only needed if USE_POSTGRES=1)
USE_POSTGRES=0
DB_NAME=configsphere
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
